# Networker runtime configuration template.
# Copy to config.yaml and fill with local values.

output_root: "~/person-intel"

local_search_paths:
  - "~/person-intel"

notification:
  type: "none"
  telegram:
    bot_token: ""
    chat_id: ""
  slack:
    webhook_url: ""
